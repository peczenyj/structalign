# Compiled binary (build artifact)
/structalign

# Vendored dependencies — resolved from go.mod/go.sum instead
/vendor/

# Go test/build output
*.out
*.test

# Coverage reports (uploaded to Codecov in CI; see Taskfile `clean`)
coverage.txt
coverage.html

# GoReleaser output
/dist/
